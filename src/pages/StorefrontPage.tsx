/**
 * Storefront Page Wrapper
 * 
 * Public page for viewing a connected account's storefront.
 */

import { Storefront } from "@/components/connect/Storefront";

const StorefrontPage = () => {
  return <Storefront />;
};

export default StorefrontPage;
